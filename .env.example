# Copy to .env, fill both empty values with distinct random passwords,
# and keep the resulting file private. Compose refuses empty values.
POSTGRES_PASSWORD=
CLAIMCORE_APP_PASSWORD=

# Optional per-checkout isolation. Connection files must use the same host port.
CLAIMCORE_COMPOSE_PROJECT=claimcore
CLAIMCORE_POSTGRES_PORT=54329
