{
  "schemaVersion": 1,
  "assembly": "ClaimCore.MigrationQualificationTests",
  "tests": [
    "ClaimCore fresh baseline qualification.fresh installation qualification.[CC-DB-001] a different calendar is refused without rewriting installation identity",
    "ClaimCore fresh baseline qualification.fresh installation qualification.[CC-DB-001] baseline DDL failure rolls back the entire namespace before commit",
    "ClaimCore fresh baseline qualification.fresh installation qualification.[CC-DB-001] concurrent different calendars cannot both win initialization",
    "ClaimCore fresh baseline qualification.fresh installation qualification.[CC-DB-001] concurrent identical initializers serialize to one complete installation",
    "ClaimCore fresh baseline qualification.fresh installation qualification.[CC-DB-001] fresh initialization atomically installs the current identity and calendar",
    "ClaimCore fresh baseline qualification.fresh installation qualification.[CC-DB-001] identical initialization and read-only verification preserve every stored byte",
    "ClaimCore fresh baseline qualification.fresh installation qualification.[CC-DB-001] verification and maintenance refuse an absent baseline without creating it",
    "ClaimCore fresh baseline qualification.unsupported installation refusal.[CC-DB-001] a matching marker does not conceal missing current integrity constraints",
    "ClaimCore fresh baseline qualification.unsupported installation refusal.[CC-DB-001] all old installation markers are refused without touching evidence or history",
    "ClaimCore fresh baseline qualification.unsupported installation refusal.[CC-DB-001] an unmarked occupied namespace is refused without deleting its contents",
    "ClaimCore fresh baseline qualification.unsupported installation refusal.[CC-DB-001] current-looking tables without a baseline marker remain unsupported",
    "ClaimCore fresh baseline qualification.unsupported installation refusal.[CC-DB-001] even an empty pre-existing namespace is not silently adopted",
    "ClaimCore fresh baseline qualification.unsupported installation refusal.[CC-DB-001] malformed or executable baseline lookalikes are not queried or repaired",
    "ClaimCore fresh baseline qualification.unsupported installation refusal.[CC-DB-001] mixing a current marker with historical metadata cannot authorize old storage",
    "ClaimCore fresh baseline qualification.unsupported installation refusal.[CC-DB-001] unknown identities and altered digests are refused with no repair"
  ]
}
