{
  "schemaVersion": 1,
  "assembly": "ClaimCore.FuzzQualificationTests",
  "tests": [
    "ClaimCore fuzz qualification.boundary decoding totality.CLI invocation framing refuses hostile input without throwing",
    "ClaimCore fuzz qualification.boundary decoding totality.canonical record decoding refuses hostile input without throwing",
    "ClaimCore fuzz qualification.boundary decoding totality.opaque cursors refuse hostile tokens without throwing",
    "ClaimCore fuzz qualification.boundary decoding totality.recovery envelope decoding refuses hostile input without throwing",
    "ClaimCore fuzz qualification.boundary decoding totality.strict JSON parsing refuses hostile input without throwing"
  ]
}
