{
  "schemaVersion": 1,
  "assembly": "ClaimCore.Tests",
  "tests": [
    "ClaimCore deterministic suite.CLI private-file security.[CC-CLI-002] concurrent source replacement yields one complete inode or a safe refusal",
    "ClaimCore deterministic suite.CLI private-file security.[CC-CLI-002] private export is exclusive, owner-only, and removes its failed partial file",
    "ClaimCore deterministic suite.CLI private-file security.[CC-CLI-002] private export rejects leaf and ancestor links without changing targets",
    "ClaimCore deterministic suite.CLI private-file security.[CC-CLI-002] private source enforces exact size, mode, ACL, and strict UTF-8",
    "ClaimCore deterministic suite.CLI private-file security.[CC-CLI-002] private source rejects symlinks, ancestor links, directories, and relative paths",
    "ClaimCore deterministic suite.CLI v3 child-process contract.CLI v3 call and session.CLI v3 call.call reports strict frame errors as one protocol JSON response",
    "ClaimCore deterministic suite.CLI v3 child-process contract.CLI v3 call and session.CLI v3 call.call retains a valid runtime request as typed configuration failure",
    "ClaimCore deterministic suite.CLI v3 child-process contract.CLI v3 call and session.CLI v3 session and hard break.removed protocol-v2 verbs are unsupported process invocations",
    "ClaimCore deterministic suite.CLI v3 child-process contract.CLI v3 call and session.CLI v3 session and hard break.session emits a local protocol frame for a blank line and exits cleanly",
    "ClaimCore deterministic suite.CLI v3 child-process contract.CLI v3 discovery processes.all structured discovery variants use the generated contract",
    "ClaimCore deterministic suite.CLI v3 child-process contract.CLI v3 discovery processes.help and plain version are configuration-free human text",
    "ClaimCore deterministic suite.CLI v3 child-process contract.CLI v3 discovery processes.structured discovery is configuration-free and contract-backed",
    "ClaimCore deterministic suite.CLI v3 protocol.CLI v3 framing.accepts a typed command draft with string revision and exact values",
    "ClaimCore deterministic suite.CLI v3 protocol.CLI v3 framing.protocol and revision rejection.rejects a numeric revision rather than risking JavaScript precision loss",
    "ClaimCore deterministic suite.CLI v3 protocol.CLI v3 framing.protocol and revision rejection.rejects protocol v2 before endpoint execution",
    "ClaimCore deterministic suite.CLI v3 protocol.CLI v3 framing.timeout and strict parser rejection.forbids caller timeouts for mutation endpoints",
    "ClaimCore deterministic suite.CLI v3 protocol.CLI v3 framing.timeout and strict parser rejection.generated raw corpus exercises every endpoint and strict rejection class",
    "ClaimCore deterministic suite.CLI v3 protocol.CLI v3 framing.timeout and strict parser rejection.missing required endpoint input is rejected before runtime access",
    "ClaimCore deterministic suite.CLI v3 protocol.CLI v3 framing.timeout and strict parser rejection.strict parser rejects duplicate frame keys and invalid UTF-8",
    "ClaimCore deterministic suite.CLI v3 protocol.CLI v3 framing.timeout and strict parser rejection.unknown and duplicate property paths never echo authored key names",
    "ClaimCore deterministic suite.CLI v3 protocol.CLI v3 generated contract.endpoint implementation inventory is exactly the generated CLI catalog",
    "ClaimCore deterministic suite.CLI v3 protocol.CLI v3 generated contract.generated schemas are strict JSON documents with the v3 declaration",
    "ClaimCore deterministic suite.CLI-v3 exit contract.[CC-REC-001] exact prepare replay wires observed acceptance and retained recovery distinctly",
    "ClaimCore deterministic suite.CLI-v3 exit contract.recovery lookup absence exits two while an empty recovery page succeeds",
    "ClaimCore deterministic suite.Contracts foundation.canonical renderers.keeps CLI and Web wire fingerprints distinct and lowercase SHA-256",
    "ClaimCore deterministic suite.Contracts foundation.canonical renderers.renders deterministic strict UTF-8 semantic and wire contracts",
    "ClaimCore deterministic suite.Contracts foundation.canonical renderers.uses Application's semantic fingerprint authority",
    "ClaimCore deterministic suite.Contracts foundation.endpoint inventories.models Web imports as raw media and keeps browser export free of CLI paths",
    "ClaimCore deterministic suite.Contracts foundation.endpoint inventories.projects every CLI v3 endpoint in its stable order",
    "ClaimCore deterministic suite.Contracts foundation.endpoint inventories.projects every HTTP v2 endpoint in its stable order",
    "ClaimCore deterministic suite.Contracts foundation.semantic projection.derives field, command, status, and rule inventories from the semantic core",
    "ClaimCore deterministic suite.Database admin private-file boundary.[CC-DB-002] schema-owner credential rejects unsafe mode, links, UTF-8, and size before database access",
    "ClaimCore deterministic suite.Domain semantic descriptors.numeric and status scalar descriptors.amount and currency descriptors own the existing exact grammars",
    "ClaimCore deterministic suite.Domain semantic descriptors.numeric and status scalar descriptors.status descriptor is the closed Domain status vocabulary",
    "ClaimCore deterministic suite.Domain semantic descriptors.ordered command inputs and prefill metadata.CORRECT_CASE describes required tagged groups and replacement fields",
    "ClaimCore deterministic suite.Domain semantic descriptors.ordered command inputs and prefill metadata.all nine command definitions are the sole command inventory",
    "ClaimCore deterministic suite.Domain semantic descriptors.ordered command inputs and prefill metadata.case reference and derived status are never command inputs",
    "ClaimCore deterministic suite.Domain semantic descriptors.ordered command inputs and prefill metadata.command inputs have exact ordered prefill sources",
    "ClaimCore deterministic suite.Domain semantic descriptors.scalar metadata ownership.field definitions expose scalar rules without a duplicate kind",
    "ClaimCore deterministic suite.Domain semantic descriptors.stable executable-rule inventory.cross-field and transition rules have unique stable identifiers",
    "ClaimCore deterministic suite.Domain semantic descriptors.text and date scalar descriptors.date descriptors define exact Gregorian input range",
    "ClaimCore deterministic suite.Domain semantic descriptors.text and date scalar descriptors.text descriptors own every text validation constraint",
    "ClaimCore deterministic suite.Hedgehog canonical records.all command shapes retain external meaning",
    "ClaimCore deterministic suite.Hedgehog canonical records.layout is insignificant but semantic changes affect identity",
    "ClaimCore deterministic suite.Hedgehog scalar boundaries.Gregorian date grammar and range",
    "ClaimCore deterministic suite.Hedgehog scalar boundaries.Unicode text and length boundaries",
    "ClaimCore deterministic suite.Hedgehog scalar boundaries.amount precision and magnitude",
    "ClaimCore deterministic suite.Hedgehog scalar boundaries.uppercase ASCII currency grammar",
    "ClaimCore deterministic suite.Hedgehog state invariants.accepted and rejected sequences preserve revision invariants",
    "ClaimCore deterministic suite.Hedgehog state invariants.reachable states match the independent availability matrix",
    "ClaimCore deterministic suite.accepted receipt first.[CC-APP-002] accepted identity conflict refuses without recovery disclosure",
    "ClaimCore deterministic suite.accepted receipt first.[CC-APP-002] accepted receipt wins after original commit confirmation is lost",
    "ClaimCore deterministic suite.accepted receipt first.[CC-APP-002] accepted replay needs no retained preparation or recovery read",
    "ClaimCore deterministic suite.accepted receipt first.[CC-APP-002] accepted-history read failure refuses before fresh preparation",
    "ClaimCore deterministic suite.accepted receipt first.[CC-APP-002] cancellation after accepted observation preserves definite receipt",
    "ClaimCore deterministic suite.accepted receipt first.[CC-REC-001] mismatched retained identity refuses without preparation metadata",
    "ClaimCore deterministic suite.administration diagnostic boundaries.administration counters retain exact 64-bit values",
    "ClaimCore deterministic suite.administration diagnostic boundaries.argument causes are precise without echoing unknown input",
    "ClaimCore deterministic suite.administration diagnostic boundaries.failed stderr cannot change commit uncertainty or append another frame",
    "ClaimCore deterministic suite.administration diagnostic boundaries.native administrative reasons have a complete closed vocabulary",
    "ClaimCore deterministic suite.administration diagnostic boundaries.output failure cannot relabel confirmed maintenance",
    "ClaimCore deterministic suite.administration diagnostic boundaries.unexpected process failure makes no not-started claim",
    "ClaimCore deterministic suite.atomic case correction.closed correction.all-keep correction is refused without changing the accepted case",
    "ClaimCore deterministic suite.atomic case correction.closed correction.closed cases may correct facts while retaining CLOSED status",
    "ClaimCore deterministic suite.atomic case correction.closed correction.equivalent scalar spelling cannot create a correction-only revision",
    "ClaimCore deterministic suite.atomic case correction.correction validation.correction cannot add a decision to an undecided case",
    "ClaimCore deterministic suite.atomic case correction.correction validation.newly asserted correction dates cannot be future dates",
    "ClaimCore deterministic suite.atomic case correction.paid correction.clearing a paid decision requires clearing its payment record",
    "ClaimCore deterministic suite.atomic case correction.paid correction.paid corrections replace related facts in one complete revision",
    "ClaimCore deterministic suite.atomic case correction.paid correction.paid decision replacement requires explicit payment action",
    "ClaimCore deterministic suite.compiler-enforced core boundary.case-work hosts exclude storage from compilation, not deployment",
    "ClaimCore deterministic suite.compiler-enforced core boundary.ordinary caller can use IClaimsCore",
    "ClaimCore deterministic suite.compiler-enforced core boundary.ordinary caller cannot create the core callback",
    "ClaimCore deterministic suite.compiler-enforced core boundary.ordinary caller cannot forge a prepared operation",
    "ClaimCore deterministic suite.compiler-enforced core boundary.ordinary caller cannot instantiate PostgreSQL storage",
    "ClaimCore deterministic suite.compiler-enforced core boundary.ordinary caller cannot see the store port",
    "ClaimCore deterministic suite.configuration projections.Compose uses the canonical digest-pinned PostgreSQL image",
    "ClaimCore deterministic suite.core outcome diagnostic emission.diagnostic specificity preserves existing cancellation precedence",
    "ClaimCore deterministic suite.core outcome diagnostic emission.provider error payloads never enter diagnostic presentation",
    "ClaimCore deterministic suite.core outcome diagnostic emission.recovery cursors retain view and operation binding causes",
    "ClaimCore deterministic suite.core outcome diagnostic emission.recovery reads emit precise ID page and cursor causes",
    "ClaimCore deterministic suite.core outcome diagnostic emission.resolve dismissal and export preserve specific admission causes",
    "ClaimCore deterministic suite.core outcome diagnostic emission.source mismatch and unsupported artifact causes remain distinct",
    "ClaimCore deterministic suite.core outcome diagnostic emission.technical commit uncertainty preserves operation identity and recovery guidance",
    "ClaimCore deterministic suite.core ownership and typed interfaces.dependency direction.Application has no transport, contract, or PostgreSQL dependency",
    "ClaimCore deterministic suite.core ownership and typed interfaces.dependency direction.Contracts projects semantic values without adapter dependencies",
    "ClaimCore deterministic suite.core ownership and typed interfaces.dependency direction.Domain has no adapter or infrastructure dependency",
    "ClaimCore deterministic suite.core ownership and typed interfaces.dependency direction.the CLI protocol cannot reach storage or composition",
    "ClaimCore deterministic suite.core ownership and typed interfaces.typed core public surface.IClaimsCore exposes only typed endpoint methods and recovery workflow",
    "ClaimCore deterministic suite.core ownership and typed interfaces.typed core public surface.transport codecs have one Contracts owner and no anonymous adapter projection",
    "ClaimCore deterministic suite.current identity and durable encoding.CORRECT_CASE uses explicit complete groups without changing the other format-3 vectors",
    "ClaimCore deterministic suite.current identity and durable encoding.all eight command encodings and identities match fixed format-3 vectors",
    "ClaimCore deterministic suite.current identity and durable encoding.snapshot-2 encoding stays independent of advisory rendering metadata",
    "ClaimCore deterministic suite.domain validation.chronology.decision before notification is rejected",
    "ClaimCore deterministic suite.domain validation.chronology.future payment is rejected",
    "ClaimCore deterministic suite.domain validation.chronology.payment before decision is rejected",
    "ClaimCore deterministic suite.domain validation.currency grammar.invalid currency case 01",
    "ClaimCore deterministic suite.domain validation.currency grammar.invalid currency case 02",
    "ClaimCore deterministic suite.domain validation.currency grammar.invalid currency case 03",
    "ClaimCore deterministic suite.domain validation.currency grammar.invalid currency case 04",
    "ClaimCore deterministic suite.domain validation.currency grammar.invalid currency case 05",
    "ClaimCore deterministic suite.domain validation.currency grammar.invalid currency case 06",
    "ClaimCore deterministic suite.domain validation.date grammar.invalid date case 01",
    "ClaimCore deterministic suite.domain validation.date grammar.invalid date case 02",
    "ClaimCore deterministic suite.domain validation.date grammar.invalid date case 03",
    "ClaimCore deterministic suite.domain validation.date grammar.invalid date case 04",
    "ClaimCore deterministic suite.domain validation.decimal grammar.invalid decimal case 01",
    "ClaimCore deterministic suite.domain validation.decimal grammar.invalid decimal case 02",
    "ClaimCore deterministic suite.domain validation.decimal grammar.invalid decimal case 03",
    "ClaimCore deterministic suite.domain validation.decimal grammar.invalid decimal case 04",
    "ClaimCore deterministic suite.domain validation.decimal grammar.invalid decimal case 05",
    "ClaimCore deterministic suite.domain validation.decimal grammar.invalid decimal case 06",
    "ClaimCore deterministic suite.domain validation.decimal grammar.invalid decimal case 07",
    "ClaimCore deterministic suite.domain validation.decimal grammar.invalid decimal case 08",
    "ClaimCore deterministic suite.domain validation.decimal grammar.invalid decimal case 09",
    "ClaimCore deterministic suite.domain validation.decimal grammar.invalid decimal case 10",
    "ClaimCore deterministic suite.domain validation.decimal grammar.invalid decimal case 11",
    "ClaimCore deterministic suite.domain validation.decimal grammar.invalid decimal case 12",
    "ClaimCore deterministic suite.domain validation.precision and round trips.all supported state views rehydrate",
    "ClaimCore deterministic suite.domain validation.precision and round trips.maximum amount stays exact",
    "ClaimCore deterministic suite.domain validation.restoration integrity.all six incomplete decision tuples are rejected on restoration",
    "ClaimCore deterministic suite.domain validation.restoration integrity.restore cannot invent a decision for payment",
    "ClaimCore deterministic suite.domain validation.restoration integrity.revision ceiling rejects terminal snapshots and prevents overflow",
    "ClaimCore deterministic suite.domain.correction state.clear payment requires no extra claims-entry data",
    "ClaimCore deterministic suite.domain.correction state.clear payment without a recorded date is rejected",
    "ClaimCore deterministic suite.domain.correction state.closed cases must reopen before edits",
    "ClaimCore deterministic suite.domain.decision editing.paid decision cannot be edited",
    "ClaimCore deterministic suite.domain.decision editing.registration amendment requires withdrawal of an existing decision",
    "ClaimCore deterministic suite.domain.decision editing.unpaid decision can be replaced",
    "ClaimCore deterministic suite.domain.identity and revisions.close and reopen preserve all business fields",
    "ClaimCore deterministic suite.domain.identity and revisions.missing case rejected",
    "ClaimCore deterministic suite.domain.identity and revisions.stale version cannot overwrite a case",
    "ClaimCore deterministic suite.domain.lifecycle meaning.decision is a tuple but is not a payment",
    "ClaimCore deterministic suite.domain.lifecycle meaning.payment is not closure",
    "ClaimCore deterministic suite.domain.lifecycle meaning.unpaid and undecided case can close",
    "ClaimCore deterministic suite.domain.payment guards.payable can exceed claim and have a different currency",
    "ClaimCore deterministic suite.domain.payment guards.payment requires decision",
    "ClaimCore deterministic suite.domain.payment guards.second full payment rejected",
    "ClaimCore deterministic suite.domain.payment guards.zero decision allowed but no fictitious zero transfer",
    "ClaimCore deterministic suite.domain.registration facts.future incident and notification are rejected",
    "ClaimCore deterministic suite.domain.registration facts.notification before incident is rejected",
    "ClaimCore deterministic suite.domain.registration facts.registration preserves the requested meanings, without invented later facts",
    "ClaimCore deterministic suite.exact retained identity.[CC-REC-001] Execute rejects a same-ID same-digest record with different canonical bytes",
    "ClaimCore deterministic suite.exact retained identity.[CC-REC-001] concurrent canonical imports classify creator and existing replay",
    "ClaimCore deterministic suite.exact retained identity.[CC-REC-001] exact Prepare retry after acceptance returns an observed receipt",
    "ClaimCore deterministic suite.exact retained identity.[CC-REC-001] exact retained Prepare retry after another commit directs Recovery",
    "ClaimCore deterministic suite.frame-local delivery diagnostics.a failed flush preserves the observed outcome and request digest",
    "ClaimCore deterministic suite.frame-local delivery diagnostics.a later input failure cannot inherit a delivered operation identity",
    "ClaimCore deterministic suite.frame-local delivery diagnostics.a malformed next frame stays independent of completed work",
    "ClaimCore deterministic suite.frame-local delivery diagnostics.a partial write preserves the current exact context without replay",
    "ClaimCore deterministic suite.frame-local delivery diagnostics.broken diagnostic output cannot loop or repeat an operation",
    "ClaimCore deterministic suite.frame-local delivery diagnostics.native outcome observation precedes serialization",
    "ClaimCore deterministic suite.frame-local delivery diagnostics.runtime acquisition failure is separate from input and dispatch failure",
    "ClaimCore deterministic suite.frame-local delivery diagnostics.uncertain import retention captures its newly known operation identity",
    "ClaimCore deterministic suite.fresh recovery format boundary.[CC-REC-001] fresh canonical and envelope formats preserve exact request identity",
    "ClaimCore deterministic suite.fresh recovery format boundary.[CC-REC-001] fresh envelopes reject digest tampering and operation substitution",
    "ClaimCore deterministic suite.fresh recovery format boundary.[CC-REC-001] historical canonical records are refused rather than re-encoded",
    "ClaimCore deterministic suite.fresh recovery format boundary.[CC-REC-001] noncanonical layouts may decode but cannot enter exact recovery retention",
    "ClaimCore deterministic suite.fresh recovery format boundary.[CC-REC-001] old envelopes and forged new envelopes cannot import old requests",
    "ClaimCore deterministic suite.generated semantic field contract.semantic command metadata.command descriptors are the only ordered input inventory",
    "ClaimCore deterministic suite.generated semantic field contract.semantic field schema.[CC-DOM-001] semantic contract renders exactly thirteen ordered field descriptors",
    "ClaimCore deterministic suite.generated semantic field contract.semantic field schema.semantic scalar metadata originates from Domain descriptors",
    "ClaimCore deterministic suite.installation business calendar.a canonical zone passes validation and fails later, on the host",
    "ClaimCore deterministic suite.installation business calendar.only a canonical zone identifier can become the installation calendar",
    "ClaimCore deterministic suite.native form contract.draft validation.flat forms reject missing extra and duplicate values",
    "ClaimCore deterministic suite.native form contract.draft validation.form binding does not trim uppercase or round business input",
    "ClaimCore deterministic suite.native form contract.draft validation.grouped correction requires complete replacements and one actual action",
    "ClaimCore deterministic suite.native form contract.field projection.field projection is exactly the thirteen native fields with absence preserved",
    "ClaimCore deterministic suite.native form contract.form binding.each declared form binds its closed command without a renderer recipe",
    "ClaimCore deterministic suite.native form contract.form binding.grouped correction binding retains each replacement scalar exactly",
    "ClaimCore deterministic suite.native form contract.form definitions.all nine forms cover the canonical commands in canonical order",
    "ClaimCore deterministic suite.native private-file ancestor race.[CC-CLI-002] descriptor-relative create and lock remain in pinned parent after ancestor swap",
    "ClaimCore deterministic suite.presentation-neutral semantic identity.copy remains bounded without becoming a machine discriminator",
    "ClaimCore deterministic suite.presentation-neutral semantic identity.discovery exposes the explicit executable-rule revision",
    "ClaimCore deterministic suite.presentation-neutral semantic identity.editorial and translated copy changes no semantic or wire identity",
    "ClaimCore deterministic suite.presentation-neutral semantic identity.embedded separators cannot collapse distinct token boundaries",
    "ClaimCore deterministic suite.presentation-neutral semantic identity.every text constraint affects each text-like scalar identity",
    "ClaimCore deterministic suite.presentation-neutral semantic identity.operational limits and explicit rule revisions remain identity-bearing",
    "ClaimCore deterministic suite.presentation-neutral semantic identity.semantic identity is invariant under host cultures",
    "ClaimCore deterministic suite.property harness policy.failure diagnostic privacy.property failures omit generated payload and control canaries",
    "ClaimCore deterministic suite.property harness policy.failure diagnostic privacy.structural assertion failures omit compared claimant values",
    "ClaimCore deterministic suite.property harness policy.property configuration.partial and additional recheck inputs fail closed",
    "ClaimCore deterministic suite.property harness policy.property configuration.required and extended profiles are explicit",
    "ClaimCore deterministic suite.published example contract.every shipped command example decodes and completes its sequential Domain transition",
    "ClaimCore deterministic suite.recovery authority closure.[CC-REC-001] durable revocation closes exact identity before and after preparation pruning",
    "ClaimCore deterministic suite.recovery authority closure.[CC-REC-001] recovery list and attempt cursors reject a different view or operation",
    "ClaimCore deterministic suite.recovery uncertainty boundaries.[CC-REC-001] cancellation after admitted attempt preserves definite acceptance",
    "ClaimCore deterministic suite.recovery uncertainty boundaries.[CC-REC-001] cancellation before attempt admission leaves the claim untouched",
    "ClaimCore deterministic suite.recovery uncertainty boundaries.[CC-REC-001] combined commit uncertainty never fabricates accepted settlement",
    "ClaimCore deterministic suite.recovery uncertainty boundaries.[CC-REC-001] commit-unknown execution is never settled",
    "ClaimCore deterministic suite.recovery uncertainty boundaries.[CC-REC-001] definite pre-commit failure is settled as a failure",
    "ClaimCore deterministic suite.recovery uncertainty boundaries.[CC-REC-001] definite stale rejection is settled without another receipt",
    "ClaimCore deterministic suite.recovery uncertainty boundaries.[CC-REC-001] receipt between observation and attempt returns exact replay",
    "ClaimCore deterministic suite.recovery uncertainty boundaries.[CC-REC-001] thrown combined commit is unresolved",
    "ClaimCore deterministic suite.recovery uncertainty boundaries.[CC-REC-001] thrown execution after admission remains unresolved",
    "ClaimCore deterministic suite.recovery uncertainty boundaries.[CC-REC-001] unknown attempt admission never executes the claim",
    "ClaimCore deterministic suite.rejection diagnostic emission.authored private values are not echoed into diagnostics",
    "ClaimCore deterministic suite.rejection diagnostic emission.domain validation emits specific scalar reasons rather than English",
    "ClaimCore deterministic suite.rejection diagnostic emission.form binding retains exact-field and correction causes",
    "ClaimCore deterministic suite.rejection diagnostic emission.parameter values come from owned scalar constraints",
    "ClaimCore deterministic suite.rejection diagnostic emission.query admission preserves page cursor and operation causes",
    "ClaimCore deterministic suite.rejection diagnostic emission.request and business-date admission retain distinct typed causes",
    "ClaimCore deterministic suite.rejection diagnostic emission.state refusals keep typed reasons and accepted data unchanged",
    "ClaimCore deterministic suite.single-owner compiled release identity.compiled source identity.CLI v3 discovery renders compiled release identity",
    "ClaimCore deterministic suite.single-owner compiled release identity.compiled source identity.native core identity comes from the SDK attributes, matching the sole source",
    "ClaimCore deterministic suite.single-owner compiled release identity.identity consumers and compatibility.a non-product assembly cannot be accepted as a compatible first-party component",
    "ClaimCore deterministic suite.single-owner compiled release identity.identity consumers and compatibility.current test and application assemblies have matching release identity",
    "ClaimCore deterministic suite.single-owner compiled release identity.version ownership.only Directory.Build.props authors product version properties",
    "ClaimCore deterministic suite.state guard and advertised actions.[CC-DOM-002] each state advertises and enforces the specified command set",
    "ClaimCore deterministic suite.state guard and advertised actions.availability edge conditions.availability does not substitute for payload validation",
    "ClaimCore deterministic suite.state guard and advertised actions.availability edge conditions.no action is advertised at the exhausted revision",
    "ClaimCore deterministic suite.typed core boundary parity.forged advisory review cannot affect accepted state",
    "ClaimCore deterministic suite.typed core boundary parity.typed and CLI-v3 decoded drafts receive the same core decision",
    "ClaimCore deterministic suite.typed core boundary parity.typed recovery storage failure and CLI wire response omit authored values",
    "ClaimCore deterministic suite.typed core boundary parity.unpaired escaped Unicode cannot alias a valid claimant request",
    "ClaimCore deterministic suite.typed core execution.typed discovery.description separates static contract from runtime context",
    "ClaimCore deterministic suite.typed core execution.typed execution.caller cancellation before admission proves no execution",
    "ClaimCore deterministic suite.typed core execution.typed execution.exact retry observes the original receipt without another commit",
    "ClaimCore deterministic suite.typed core execution.typed execution.execute commits an open case and get exposes derived actions",
    "ClaimCore deterministic suite.typed core execution.typed execution.execute retains identity when preparation state is unknown",
    "ClaimCore deterministic suite.typed core execution.typed execution.idempotency and concurrency rejections.operation-ID content conflict is an explicit typed rejection",
    "ClaimCore deterministic suite.typed core execution.typed execution.idempotency and concurrency rejections.stale command is rejected before recovery attempt admission",
    "ClaimCore deterministic suite.typed core facade.preparation.prepare derives all review values from domain state",
    "ClaimCore deterministic suite.typed core facade.queries.missing history is a successful explicit lookup absence",
    "ClaimCore deterministic suite.typed core facade.queries.query cancellation never enters a storage read",
    "ClaimCore deterministic suite.typed core facade.recovery.[CC-REC-001] existing receipt with different bytes cannot masquerade as replay",
    "ClaimCore deterministic suite.typed core facade.recovery.dismissed recovery is refused before an attempt starts",
    "ClaimCore deterministic suite.typed core facade.recovery.recovery export requires the exact retained digest",
    "ClaimCore deterministic suite.typed core facade.recovery.recovery resolve executes an exact retained preparation once",
    "ClaimCore deterministic suite.typed core outcome diagnostics.adapter failures are distinct from core results on every CLI endpoint",
    "ClaimCore deterministic suite.typed core outcome diagnostics.all lifecycle refusals have identical CLI and Web diagnostics",
    "ClaimCore deterministic suite.typed core outcome diagnostics.all native faults have identical CLI and Web diagnostics",
    "ClaimCore deterministic suite.typed core outcome diagnostics.ambient locale does not change core outcome diagnostics",
    "ClaimCore deterministic suite.typed core outcome diagnostics.core reasons cannot carry messages arbitrary parameters or writable guidance",
    "ClaimCore deterministic suite.typed core outcome diagnostics.fault and recovery diagnostic inventories participate in semantic identity",
    "ClaimCore deterministic suite.typed core outcome diagnostics.fault identities and guidance match the reviewed closed vocabulary",
    "ClaimCore deterministic suite.typed core outcome diagnostics.recovery identities and guidance match the reviewed closed vocabulary",
    "ClaimCore deterministic suite.typed core outcome diagnostics.semantic diagnostic families are disjoint complete and parameterless",
    "ClaimCore deterministic suite.typed core queries.query validation and cancellation.history rejects malformed opaque cursors",
    "ClaimCore deterministic suite.typed core queries.query validation and cancellation.query cancellation returns no synthetic success or failure",
    "ClaimCore deterministic suite.typed core queries.typed lookup outcomes.empty history is explicit only after the case exists",
    "ClaimCore deterministic suite.typed core queries.typed lookup outcomes.get distinguishes a missing reference from a rejected request",
    "ClaimCore deterministic suite.typed core queries.typed lookup outcomes.operation observation distinguishes a missing operation from a store fault",
    "ClaimCore deterministic suite.typed pagination boundaries.case listing returns request-bounded stable pages without loss or overlap",
    "ClaimCore deterministic suite.typed pagination boundaries.history cursors preserve detail mode and continue from the last returned revision",
    "ClaimCore deterministic suite.typed protocol diagnostic boundaries.oversized NDJSON frames drain before admitting the next frame",
    "ClaimCore deterministic suite.typed protocol diagnostic boundaries.protocol vocabulary is closed complete and presentation-free",
    "ClaimCore deterministic suite.typed protocol diagnostic boundaries.untrusted property names cannot enter diagnostic locations",
    "ClaimCore deterministic suite.typed protocol diagnostic boundaries.wrong scalar kinds are typed refusals rather than escaping exceptions",
    "ClaimCore deterministic suite.typed recovery cancellation.[CC-REC-001] cancellation during receipt observation returns cancelled read",
    "ClaimCore deterministic suite.typed recovery cancellation.[CC-REC-001] cancellation during recovery inspect hides receipt result",
    "ClaimCore deterministic suite.typed recovery cancellation.[CC-REC-001] cancellation during resolve observation prevents attempt",
    "ClaimCore deterministic suite.typed recovery cancellation.[CC-REC-001] cancelled combined execution is definite before commit",
    "ClaimCore deterministic suite.typed recovery cancellation.[CC-REC-001] commit-start dismissal failure remains state unknown",
    "ClaimCore deterministic suite.typed recovery cancellation.[CC-REC-001] commit-start import retention failure remains state unknown",
    "ClaimCore deterministic suite.typed recovery cancellation.[CC-REC-001] definite attempt cancellation prevents claim execution",
    "ClaimCore deterministic suite.typed recovery cancellation.[CC-REC-001] definite dismiss cancellation remains actionable",
    "ClaimCore deterministic suite.typed recovery cancellation.[CC-REC-001] definite import retention cancellation does not submit",
    "ClaimCore deterministic suite.typed recovery cancellation.[CC-REC-001] definite retain cancellation becomes cancelled before admission",
    "ClaimCore deterministic suite.typed rejection diagnostics.CLI and Web project identical diagnostics without changing rejection exit semantics",
    "ClaimCore deterministic suite.typed rejection diagnostics.ambient culture never selects rejection identity or parameter values",
    "ClaimCore deterministic suite.typed rejection diagnostics.diagnostic arguments match the closed safe parameter schema",
    "ClaimCore deterministic suite.typed rejection diagnostics.diagnostic identities and parameter shapes participate in all fingerprints",
    "ClaimCore deterministic suite.typed rejection diagnostics.diagnostic parameter names and both bounds participate in semantic identity",
    "ClaimCore deterministic suite.typed rejection diagnostics.every closed identity has one explicit token and a complete projection",
    "ClaimCore deterministic suite.typed rejection diagnostics.field targets are closed and use public revision vocabulary",
    "ClaimCore deterministic suite.typed rejection diagnostics.native rejection payloads cannot contain free text or arbitrary bags",
    "ClaimCore deterministic suite.typed rejection diagnostics.specific causes remain distinct under coarse codes and preserve actions"
  ]
}
